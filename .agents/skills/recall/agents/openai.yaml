interface:
  display_name: "Recall"
  short_description: "Repo-specific patterns and workflows for recall"
  default_prompt: "Use the recall repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true