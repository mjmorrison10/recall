---
approved: 2026-07-22
---

# RECALL: speed up AI (disable Gemini thinking + trim token budget)

## Goal
Owner reports AI runs (upload transcription, auto-scout, Top Clips, AI Compose)
take an oddly long time. Root cause: `gemini-flash-latest` resolves to Gemini
2.5 Flash, which has "thinking" ENABLED by default — it spends several seconds
generating hidden reasoning tokens on every call, and those tokens also count
against `maxOutputTokens`. None of RECALL's calls benefit from hidden reasoning
(they're transcription + structured JSON labeling).

## What changed
- **llm.js** — add `thinkingConfig: { thinkingBudget: 0 }` to `generationConfig`
  in both `geminiGenerateText` (line 201) and `geminiGenerateFromMedia`
  (line 244). Disables thinking for every Gemini call (text + media).
- **topclips.js:440** (`aiPass`) — `maxTokens: 8000 → 4000`. The output is ≤20
  short JSON clip labels; 8000 was headroom for thinking tokens that are now
  off. Keeps ~2x margin.
- Left alone: AI Compose 600, transcription 16000 (genuinely needs the budget),
  Files-API poll interval, and the inherent transcribe→then→scout two-call
  pipeline. OpenRouter path untouched.

## Files touched
`llm.js`, `topclips.js`. No schema/storage/UI changes.

## Rollback
Delete the one `thinkingConfig` line per site (2) and restore 8000. If Google
ever repoints the alias at a model that rejects `thinkingBudget:0` (Pro-class),
calls would 400 — same one-line fix.

## Verification — PASS (headless Playwright, 2026-07-22)
Drove `window.LLMProvider.generateText` with a fake key while intercepting the
Gemini endpoint; 7/7 green:
- request `generationConfig.thinkingConfig.thinkingBudget === 0`
- `maxOutputTokens` passes through (4000)
- `responseMimeType: application/json` still set under jsonMode
- canned response round-trips, no page errors.
Script: scratchpad/recall-thinking-verify.mjs.

## Audit (post-execution)
- PLAN: written + approved (this file). PASS
- EXECUTE: llm.js (2 sites) + topclips.js edited as planned. PASS
- VERIFY: 7/7 headless assertions green. PASS
- SHIP: push → PR → squash-merge → live-URL cache-busted poll (below).
